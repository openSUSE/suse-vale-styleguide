---
extends: substitution
message: Use '%s' instead of '%s'
ignorecase: true
level: error
swap:
  32 Bit|32 bit|32-Bit|32Bit|32bit: 32-bit
  64 Bit|64 bit|64-Bit|64Bit|64bit: 64-bit
  back end|backend: back-end
  boot disc|boot-disk|bootdisk: boot disk
  boot-loader|bootloader: boot loader
  change log|change-log|ChangeLog: changelog
  check-box|checkbox: check box
  check list|check-list: checklist
  check-mark|checkmark|tick|tick mark: check mark
  chip set|chip-set: chipset
  code stream: codestream
  crashdump: crash dump
  file-name|filename: file name
  file-system|filesystem: file system
  frame buffer|frame-buffer: framebuffer
  hard-link|hardlink: hard link
  kernel-space|kernelspace: kernel space
  life cycle|life-cycle: lifecycle
  loop back device|loop-back device: loopback device
  meta data|meta-data|metadatas: metadata
  mount-point|mountpoint: mount point
  name-server|nameserver: name server
  name space|name-space: namespace
  patch-level, patchlevel: patch level
  path-name|pathname: path name
  pop up|popup: pop-up
  printer queue|printing queue: print queue
  printer spooler|printing spooler: print spooler
  run level|run-level: runlevel
  run time|run-time: runtime
  scroll-bar|scroll bar: scrollbar
  stand alone|standalone: stand-alone
  solid state dics|solid-state disk drive|solid-state disk|solid state drive|solidstate drive: solid-state drive
  status bar|status-bar: statusbar
  sub directory|sub-directory: subdirectory
  sub menu|sub-menu: submenu
  sub system|sub-system: subsystem
  symlink: symbolic link
  task bar|task-bar: taskbar
  time-stamp|timestamp: time stamp
  title bar|title-bar: titlebar
  tool bar|tool-bar: toolbar
  tool chain|tool-chain: toolchain
  tool tip|tool-tip: tooltip
  use-case|usecase: use case
  user-name|username: user name
  
  